spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/huludao_mahjong}
    username: ${DB_PROD_USERNAME}
    password: ${DB_PROD_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
